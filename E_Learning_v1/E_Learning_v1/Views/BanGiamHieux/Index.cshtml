@model IEnumerable<E_Learning_v1.Model.BanGiamHieu>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TenBGH)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TenBGH)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MaBGH">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MaBGH">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MaBGH">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
